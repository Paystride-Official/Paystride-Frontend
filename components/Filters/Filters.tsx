import React from "react";

type Props = {};

const Filters = (props: Props) => {
  return (
    <div>
      <button className="border border-solid border-[#DDE2FD] px-4 py-1 rounded-md">
        Filters
      </button>
    </div>
  );
};

export default Filters;
